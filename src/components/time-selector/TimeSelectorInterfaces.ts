export interface ICheckbox {
    order: number
    label: string
    checked: boolean
}

export interface IDayCard {
    order: number
    name: string
}

export interface ITimestamp {
    startTime: string
    endTime: string
}